# 电商 AI Agent 系统设计文档

> 编制时间：2026年9月19日
> 版本：v1.1
> 更新内容：补充 RAG 知识库、向量推荐、Embedding模型设计

---

## 一、项目概述

### 1.1 项目定位

构建一个面向电商场景的 AI Agent 系统，支持多角色（卖家、客服、买家）使用，通过自然语言交互完成数据分析、客服处理、选品推荐、内容创作等任务。

### 1.2 核心价值

| 角色 | 核心价值 |
|------|----------|
| **卖家/运营** | 数据分析自动化，决策效率提升 |
| **客服** | AI辅助回复，响应速度提升 |
| **买家** | 智能问答，购物体验优化 |

### 1.3 设计原则

- **一个后端，多个入口**：统一Agent核心，不同角色不同界面
- **权限隔离**：不同角色只能访问对应功能和数据
- **渐进式发展**：MVP先行，逐步扩展能力

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层                                │
│                                                             │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │
│   │ 卖家后台    │  │ 客服工作台  │  │ 买家端          │   │
│   │ (Web)       │  │ (Web)       │  │ (小程序/H5)     │   │
│   └─────────────┘  └─────────────┘  └─────────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │ HTTP/WebSocket
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    API 网关层                                │
│              FastAPI + JWT 认证                              │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                  Agent 核心层                                │
│                                                             │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐   │
│   │  LangChain  │───▶│  LangGraph  │───▶│  LangSmith  │   │
│   │  基础框架   │    │  工作流编排  │    │  监控追踪   │   │
│   └─────────────┘    └─────────────┘    └─────────────┘   │
│                                                             │
│   ┌─────────────────────────────────────────────────────┐  │
│   │                    子Agent层                        │  │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │  │
│   │  │ 数据分析 │ │ 客服处理 │ │ 选品推荐 │ │内容创作│ │  │
│   │  └──────────┘ └──────────┘ └──────────┘ └────────┘ │  │
│   └─────────────────────────────────────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    LLM 模型层                                │
│                                                             │
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐   │
│   │ DeepSeek V3 │    │ Qwen 2.5    │    │ GPT-4o      │   │
│   │ (主力/开源) │    │ (备选)      │    │ (兜底)      │   │
│   └─────────────┘    └─────────────┘    └─────────────┘   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据层                                    │
│                                                             │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│   │PostgreSQL│  │  Redis   │  │  Milvus  │  │  MinIO   │  │
│   │ 业务数据 │  │ 短期记忆 │  │ 长期记忆 │  │ 文件存储 │  │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 数据流

```
用户输入（自然语言）
        │
        ▼
┌─────────────────┐
│   意图识别      │  LangGraph 判断用户想干什么
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
 简单问题   复杂问题
    │         │
    ▼         ▼
 直接回答   调用工具
              │
              ▼
         获取数据
              │
              ▼
         生成回答
```

---

## 三、入口设计

### 3.1 角色与入口

| 角色 | 入口 | 面向对象 | 核心功能 |
|------|------|----------|----------|
| **卖家/运营** | 卖家后台（Web） | 店铺管理者 | 数据分析、商品管理、营销工具 |
| **客服人员** | 客服工作台（Web） | 店铺员工 | 接待买家、处理售后 |
| **买家** | 店铺页面（小程序/H5） | 消费者 | 商品咨询、订单查询、售后申请 |

### 3.2 买家与客服的关系

```
买家咨询
    │
    ▼
┌─────────────────┐
│   AI 客服       │  买家直接对话的对象
└────────┬────────┘
         │
    能回答 → 直接回复
    不能回答 → 转人工（客服工作台）
```

**关键点：**
- 买家只有一个入口（店铺页面的"联系客服"）
- 买家不需要知道背后是AI还是真人
- 客服人员在后台工作台处理AI无法解决的问题

### 3.3 客服工作台

```
┌─────────────────────────────────────────────────────────────┐
│  客服工作台                           [在线 | 今日接待: 47]  │
├────────────────────┬────────────────────────────────────────┤
│  待处理 (5)        │  当前会话: 买家-李女士                  │
│  ─────────────     │  ────────────────────────────────      │
│  🔴 李女士 - 退款  │  [买家]: 我想退货，尺码买小了            │
│  🟡 王先生 - 物流  │  [AI建议]: 可引导申请退货退款            │
│  🟢 赵同学 - 咨询  │                                        │
│  ...               │  [输入框]                    [发送]    │
└────────────────────┴────────────────────────────────────────┘
```

---

## 四、数据库设计

### 4.1 表结构概览

共6张核心表：

| 表名 | 用途 | 必要性 |
|------|------|--------|
| users | 用户信息、角色 | ✅ |
| shops | 店铺信息 | ✅ |
| products | 商品信息 | ✅ |
| orders | 订单主表 | ✅ |
| order_items | 订单明细 | ✅ |
| chat_messages | 客服聊天记录（归档） | ✅ |

### 4.2 详细表结构

#### users（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| username | VARCHAR(50) | 用户名 |
| password_hash | VARCHAR(255) | 密码哈希 |
| role | ENUM | 角色：seller/customer_service/buyer/admin |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(100) | 邮箱 |
| shop_id | INT | 所属店铺（卖家/客服用） |
| created_at | DATETIME | 注册时间 |

#### shops（店铺表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| shop_name | VARCHAR(100) | 店铺名称 |
| owner_id | INT | 店主（外键→users） |
| platform | VARCHAR(50) | 平台：shopify/taobao/self_built |
| api_key | VARCHAR(255) | 平台API密钥（加密存储） |
| status | ENUM | active/inactive |
| created_at | DATETIME | 创建时间 |

#### products（商品表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| shop_id | INT | 所属店铺 |
| product_name | VARCHAR(200) | 商品名称 |
| sku | VARCHAR(100) | SKU编码 |
| category | VARCHAR(100) | 分类 |
| price | DECIMAL(10,2) | 单价 |
| cost | DECIMAL(10,2) | 成本价（卖家可见） |
| stock | INT | 库存数量 |
| status | ENUM | on_sale/off_sale/sold_out |
| description | TEXT | 商品描述 |
| created_at | DATETIME | 上架时间 |

#### orders（订单表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| order_no | VARCHAR(50) | 订单编号（唯一） |
| shop_id | INT | 所属店铺 |
| buyer_id | INT | 买家ID |
| total_amount | DECIMAL(10,2) | 订单总额 |
| status | ENUM | pending/paid/shipped/completed/refunded |
| created_at | DATETIME | 下单时间 |
| paid_at | DATETIME | 支付时间 |
| shipped_at | DATETIME | 发货时间 |

#### order_items（订单明细表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| order_id | INT | 订单ID |
| product_id | INT | 商品ID |
| quantity | INT | 数量 |
| unit_price | DECIMAL(10,2) | 下单时单价 |
| subtotal | DECIMAL(10,2) | 小计 |

#### chat_messages（客服聊天表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| session_id | VARCHAR(50) | 会话ID |
| shop_id | INT | 所属店铺 |
| buyer_id | INT | 买家ID |
| message_type | ENUM | user/agent/human |
| content | TEXT | 消息内容 |
| metadata | JSON | 意图、情绪、工具调用等 |
| created_at | DATETIME | 时间 |

### 4.3 表关系图

```
users (用户)
  │
  ├──▶ shops (店铺)
  │      │
  │      ├──▶ products (商品)
  │      │      │
  │      │      └──▶ order_items (订单明细)
  │      │
  │      ├──▶ orders (订单)
  │      │      │
  │      │      └──▶ order_items (订单明细)
  │      │
  │      └──▶ chat_messages (客服聊天)
```

---

## 五、记忆系统与 RAG

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      记忆与知识系统                           │
│                                                             │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│   │  Redis   │  │  Milvus  │  │PostgreSQL│  │LangSmith │  │
│   │ 短期记忆 │  │ 向量存储 │  │ 结构化数据│  │ 链路追踪 │  │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 三层记忆架构

| 层级 | 存储 | 用途 | 生命周期 |
|------|------|------|----------|
| **短期记忆** | Redis | 当前会话上下文 | 30分钟（会话结束清理） |
| **长期记忆 + RAG** | Milvus（向量库） | 用户画像、对话摘要、商品向量、知识库 | 永久 |
| **结构化日志** | LangSmith | 调用链路、性能监控 | 按策略归档 |

### 5.3 记忆流转

```
用户消息
    │
    ├──▶ Redis（当前会话上下文）
    │
    ├──▶ Milvus（对话结束时，LLM总结后存储为长期记忆）
    │
    └──▶ Milvus（商品向量，用于语义检索推荐）
```

### 5.4 Milvus 存储内容（四个 Collection）

| Collection | 内容 | 用途 |
|------------|------|------|
| **product_vectors** | 商品向量 + 元数据 | 商品推荐、相似商品、向量搜索 |
| **user_vectors** | 用户画像向量 | 个性化推荐 |
| **faq_vectors** | FAQ知识向量 | 问题匹配、客服辅助 |
| **conversation_vectors** | 对话摘要向量 | 历史会话检索、知识沉淀 |

### 5.5 RAG 知识库

**为什么需要 RAG？**

Agent 有两种获取信息的方式：

| 方式 | 用途 | 举例 |
|------|------|------|
| **工具调用** | 获取实时数据 | "A商品卖了多少？" → 查数据库 |
| **RAG 检索** | 获取知识文档 | "这个耳机怎么用？" → 查说明书 |

**RAG 在电商场景的应用：**

| 角色 | 场景 | RAG 检索内容 |
|------|------|--------------|
| **买家** | 商品咨询 | 商品详情、使用说明、规格参数 |
| **买家** | 售后问题 | 退换货政策、保修条款、常见问题 |
| **客服** | 回复买家 | FAQ知识库、话术模板 |
| **客服** | 处理售后 | 平台规则、赔付标准 |
| **卖家** | 运营咨询 | 平台规则、运营指南 |

**RAG 知识库内容：**

| 知识库 | 内容 | 更新频率 |
|--------|------|----------|
| **商品知识库** | 商品详情、参数、说明书 | 商品上架时 |
| **FAQ知识库** | 常见问题及答案 | 定期更新 |
| **政策知识库** | 退换货政策、平台规则 | 政策变更时 |
| **话术知识库** | 客服回复模板、优秀案例 | 持续积累 |

**RAG 工作流程：**

```
用户: "这个耳机支持苹果手机吗？"
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 1: 意图判断 → 需要检索商品知识库                        │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: 向量检索（Milvus）                                  │
│                                                             │
│ 将问题向量化 → 检索最相关的文档片段                          │
│                                                             │
│ 结果：                                                      │
│ - "支持蓝牙5.3，兼容iOS 14+和Android 10+"                 │
│ - "苹果手机需在设置中开启蓝牙配对"                          │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: LLM 整合回答                                        │
│                                                             │
│ "这款耳机支持苹果手机，需要iOS 14以上系统版本。             │
│  首次使用请在蓝牙设置中配对连接。"                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 六、监控方案

### 6.1 工具选择

| 工具 | 用途 | 阶段 |
|------|------|------|
| **LangSmith** | Agent链路追踪、Token消耗 | 立即使用 |
| **Prometheus + Grafana** | 系统监控、性能指标 | 上线后 |
| **OpenTelemetry** | 分布式追踪标准 | 不直接使用 |

### 6.2 LangSmith 追踪内容

- 每一步的输入输出
- 工具调用记录
- Token消耗统计
- 耗时分析
- 错误定位

---

## 七、Agent 核心设计

### 7.1 调度 Agent（Router）

**职责：** 接收用户输入，判断意图，分配给对应子Agent

**路由逻辑：**
```
用户输入 + 用户角色
    │
    ▼
┌─────────────────┐
│   意图识别      │
│   实体提取      │
│   角色校验      │
└────────┬────────┘
         │
    ┌────┴────┬────────────┬────────────┐
    ▼         ▼            ▼            ▼
 数据分析  客服处理      选品推荐     内容创作
    │         │            │            │
    └─────────┴────────────┴────────────┘
                       │
                       ▼
                  兜底处理
```

### 7.2 五个子Agent

| 子Agent | 职责 | 适用角色 | 核心工具 |
|---------|------|----------|----------|
| **数据分析Agent** | 报表、趋势、异常检测 | 卖家 | query_sales, query_inventory, query_return_rate |
| **客服处理Agent** | 解答咨询、处理售后 | 客服/买家 | query_order, query_logistics, create_refund |
| **选品推荐Agent** | 趋势发现、机会识别 | 卖家 | search_product_vector, get_category_trend |
| **内容创作Agent** | 文案生成、描述优化 | 卖家 | get_product_info, get_audience |
| **知识问答Agent** | 商品咨询、FAQ回答 | 买家/客服 | rag_search |

### 7.3 子Agent工作流程

```
每个子Agent遵循相同模式：

输入（用户意图 + 参数）
    │
    ▼
┌─────────────────┐
│  理解意图       │  LLM解析用户需求
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  选择工具       │  根据需求选择合适的工具
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  执行工具       │  调用工具获取数据/知识
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  生成回答       │  整合数据，生成自然语言
└─────────────────┘
```

### 7.4 向量推荐流程（选品推荐Agent）

```
用户: "适合通勤用的降噪耳机，预算300以内"
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 1: 意图向量化                                          │
│                                                             │
│ 提取关键语义：                                               │
│ • 场景：通勤                                                │
│ • 功能：降噪                                                │
│ • 品类：耳机                                                │
│ • 价格：300以内                                             │
│                                                             │
│ LLM 生成查询向量: [0.12, 0.85, 0.23, ...]                  │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 2: 向量检索（Milvus product_vectors）                  │
│                                                             │
│ 在商品向量库中找最相似的商品：                               │
│                                                             │
│ Top 1: ProSound降噪耳机 (相似度: 0.94)                     │
│        - 通勤场景, 主动降噪40dB, ¥269                      │
│                                                             │
│ Top 2: 享乐蓝牙耳机 (相似度: 0.89)                         │
│        - 商务通勤, 轻量设计, ¥299                          │
│                                                             │
│ Top 3: 飞羽降噪耳机 (相似度: 0.85)                         │
│        - 地铁专用, 长续航, ¥259                            │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 3: 结果过滤                                            │
│                                                             │
│ 应用硬性条件筛选：                                           │
│ • 价格 ≤ 300 ✓                                             │
│ • 库存 > 0 ✓                                               │
│ • 评分 ≥ 4.0 ✓                                             │
└─────────────────────────┬───────────────────────────────────┘
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Step 4: 生成推荐回答                                        │
│                                                             │
│ "根据您的需求，推荐以下几款：                                │
│                                                             │
│  1. ProSound降噪耳机 ¥269 ⭐ 4.8分                        │
│     主动降噪40dB，特别适合地铁通勤，30h长续航               │
│                                                             │
│  2. 享乐蓝牙耳机 ¥299 ⭐ 4.7分                            │
│     轻量设计仅28g，商务通勤首选                             │
│                                                             │
│  3. 飞羽降噪耳机 ¥259 ⭐ 4.6分                            │
│     性价比之选，降噪效果不错"                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 八、工具层设计

### 8.1 工具清单

#### 数据分析类

| 工具名 | 功能 | 参数 |
|--------|------|------|
| query_sales | 查询销量/销售额 | product, period, group_by |
| query_inventory | 查询库存 | product, threshold |
| query_return_rate | 查询退货率 | product, period |
| query_profit | 查询利润 | product, period |
| query_top_products | 查询热销商品 | period, limit, sort_by |

#### 订单相关类

| 工具名 | 功能 | 参数 |
|--------|------|------|
| query_order | 查询订单详情 | order_no |
| query_logistics | 查询物流状态 | order_no |
| query_order_list | 查询订单列表 | buyer_id, status, period |

#### 客服操作类

| 工具名 | 功能 | 参数 |
|--------|------|------|
| create_refund | 创建退款单 | order_no, reason, amount |
| update_order_status | 更新订单状态 | order_no, new_status |
| get_faq | 获取常见问题 | question |

#### 向量搜索类（RAG + 推荐）

| 工具名 | 功能 | 参数 |
|--------|------|------|
| search_product_vector | 向量搜索商品 | query_text, filters, top_k |
| search_similar_products | 查找相似商品 | product_id, top_k |
| rag_search | 知识库检索 | query_text, knowledge_base, top_k |
| search_faq | 搜索FAQ | question, category |
| get_user_preference | 获取用户偏好 | user_id |

#### 选品推荐类

| 工具名 | 功能 | 参数 |
|--------|------|------|
| get_category_trend | 品类趋势 | period, sort_by |
| search_trending | 搜索热门商品 | keyword, period |
| analyze_competitor | 竞品分析 | product_name |

#### 内容创作类

| 工具名 | 功能 | 参数 |
|--------|------|------|
| get_product_info | 获取商品详情 | product_id 或 product_name |
| get_audience | 获取受众画像 | category |

### 8.2 向量搜索工具详解

#### search_product_vector（向量搜索商品）

```python
def search_product_vector(
    query_text: str,           # 用户需求描述，如"适合通勤的降噪耳机"
    filters: dict = None,      # 筛选条件，如 {"price_max": 300, "category": "耳机"}
    top_k: int = 5             # 返回数量
):
    """
    根据自然语言需求向量搜索商品
    
    流程：
    1. 将 query_text 通过 Embedding 模型转成向量
    2. 在 Milvus product_vectors 中检索最相似的商品
    3. 应用 filters 筛选
    4. 返回 top_k 个结果
    """
    # 1. 向量化查询
    query_vector = embedding_model.encode(query_text)
    
    # 2. Milvus 检索
    results = milvus_client.search(
        collection_name="product_vectors",
        query_vector=query_vector,
        limit=top_k * 2,  # 多取一些，后面筛选
        output_fields=["product_id", "product_name", "category", "price", "features"]
    )
    
    # 3. 应用筛选条件
    filtered = apply_filters(results, filters)
    
    return filtered[:top_k]
```

#### rag_search（知识库检索）

```python
def rag_search(
    query_text: str,           # 用户问题
    knowledge_base: str,       # 知识库：product/faq/policy/script
    top_k: int = 3             # 返回数量
):
    """
    从知识库中检索相关文档片段
    
    流程：
    1. 将问题向量化
    2. 在对应的 Milvus Collection 中检索
    3. 返回最相关的文档片段
    """
    collection_map = {
        "product": "product_knowledge_vectors",
        "faq": "faq_vectors",
        "policy": "policy_vectors",
        "script": "script_vectors"
    }
    
    query_vector = embedding_model.encode(query_text)
    
    results = milvus_client.search(
        collection_name=collection_map[knowledge_base],
        query_vector=query_vector,
        limit=top_k,
        output_fields=["content", "source", "last_updated"]
    )
    
    return results
```

### 8.3 商品向量化流程

```
商品上架
    │
    ▼
┌─────────────────┐
│ 提取商品信息    │  名称、描述、特性、场景、价格
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 构造文本        │  拼接成适合向量化的文本
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Embedding       │  通过模型转成向量
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 存入 Milvus     │  向量 + 元数据一起存储
└─────────────────┘
```

**商品文本构造示例：**
```
ProSound降噪耳机 主动降噪40dB 适合通勤地铁 无线蓝牙5.3 30小时续航 价格269元
```

### 8.4 权限控制

| 角色 | 可调用工具 | 限制 |
|------|------------|------|
| buyer | query_order, query_logistics, search_product_vector, rag_search, get_faq, create_refund | 只能查自己的订单 |
| customer_service | 订单类、客服操作类、rag_search | 不能改商品信息 |
| seller | 所有工具 | 无限制 |

### 8.5 错误处理

```python
工具执行流程：
1. 参数校验 → 失败返回错误提示
2. 权限校验 → 失败返回无权限提示
3. 执行工具 → 成功返回结果
4. 异常处理 → 超时/数据库错误等统一处理
```

---

## 九、技术选型

### 9.1 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **Agent框架** | LangChain + LangGraph | 基础框架 + 工作流编排 |
| **监控** | LangSmith | Agent链路追踪 |
| **LLM** | DeepSeek V3（主力）/ Qwen 2.5（备选） | 开源、性价比高 |
| **Embedding** | text-embedding-3-small / BGE-M3 | 向量化模型 |
| **后端** | FastAPI | Python Web框架 |
| **数据库** | PostgreSQL | 业务数据存储 |
| **缓存** | Redis | 短期记忆、会话缓存 |
| **向量库** | Milvus | 长期记忆、RAG、商品推荐 |
| **文件存储** | MinIO | 图片、文档存储 |

### 9.2 Embedding 模型选择

| 模型 | 维度 | 特点 | 推荐场景 |
|------|------|------|----------|
| **text-embedding-3-small** | 1536 | OpenAI出品、效果好 | 通用场景 |
| **BGE-M3** | 1024 | 开源、支持多语言 | 国内部署 |
| **text2vec-chinese** | 768 | 中文优化、轻量 | 中文为主场景 |

**推荐：** 国内部署用 BGE-M3，效果好且无需翻墙。

### 9.3 LangGraph 的作用

**为什么需要 LangGraph？**

普通LangChain是线性的：
```
输入 → LLM → 工具 → LLM → 输出
```

电商场景需要灵活的流程控制：
```
用户输入
    │
    ▼
  意图判断 ──┬──▶ 数据分析Agent ──▶ 输出
             │
             ├──▶ 客服处理Agent ──▶ 输出
             │
             ├──▶ 选品推荐Agent ──▶ 向量检索 ──▶ 输出
             │
             └──▶ 知识问答Agent ──▶ RAG检索 ──▶ 输出
```

LangGraph 用状态图来编排这些分支、循环和条件判断。

### 9.4 三个工具的分工

| 工具 | 职责 | 比喻 |
|------|------|------|
| **LangChain** | 基础能力：Prompt、LLM调用、工具定义 | 积木 |
| **LangGraph** | 工作流编排：多步骤、分支、循环、状态 | 搭积木的蓝图 |
| **LangSmith** | 可观测性：追踪、日志、调试 | 监控摄像头 |

---

## 十、项目结构

```
ecommerce-agent/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI 入口
│   │
│   ├── agents/                 # Agent 核心
│   │   ├── __init__.py
│   │   ├── router.py           # 路由Agent（LangGraph）
│   │   ├── data_analyst.py     # 数据分析Agent
│   │   ├── customer_service.py # 客服Agent
│   │   ├── recommender.py      # 推荐Agent
│   │   ├── knowledge_qa.py     # 知识问答Agent（RAG）
│   │   └── writer.py           # 内容创作Agent
│   │
│   ├── tools/                  # 工具层
│   │   ├── __init__.py
│   │   ├── database.py         # 数据库工具
│   │   ├── order_tools.py      # 订单相关工具
│   │   ├── product_tools.py    # 商品相关工具
│   │   ├── vector_tools.py     # 向量搜索工具
│   │   ├── rag_tools.py        # RAG检索工具
│   │   └── external_api.py     # 外部API
│   │
│   ├── memory/                 # 记忆系统
│   │   ├── __init__.py
│   │   ├── short_term.py       # Redis 短期记忆
│   │   ├── long_term.py        # Milvus 长期记忆
│   │   └── rag.py              # RAG检索模块
│   │
│   ├── vector/                 # 向量处理
│   │   ├── __init__.py
│   │   ├── embedding.py        # Embedding模型封装
│   │   ├── product_index.py    # 商品向量索引
│   │   └── knowledge_index.py  # 知识库向量索引
│   │
│   ├── models/                 # 数据模型
│   │   ├── __init__.py
│   │   └── schemas.py
│   │
│   └── config/                 # 配置
│       ├── __init__.py
│       └── settings.py
│
├── prompts/                    # Prompt 模板
│   ├── router.txt
│   ├── data_analyst.txt
│   ├── customer_service.txt
│   ├── recommender.txt
│   └── knowledge_qa.txt
│
├── data/                       # 数据文件
│   ├── mock/                   # 模拟数据
│   └── knowledge/              # 知识库文档
│       ├── products/           # 商品知识
│       ├── faq/                # FAQ
│       └── policy/             # 政策规则
│
├── tests/                      # 测试
├── requirements.txt
└── README.md
```

---

## 十一、依赖清单

```txt
# requirements.txt

# 核心框架
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0
langsmith>=0.1.0

# LLM
openai>=1.0.0
dashscope>=1.20.0

# 数据库
sqlalchemy>=2.0.0
asyncpg>=0.30.0
redis>=5.0.0
pymilvus>=2.4.0

# Web框架
fastapi>=0.115.0
uvicorn>=0.32.0
python-dotenv>=1.0.0

# 工具
pandas>=2.2.0
matplotlib>=3.9.0
```

---

## 十二、实施计划

### Phase 1: MVP（1-2周）

**目标：** 跑通核心流程

| 任务 | 说明 |
|------|------|
| 搭建项目骨架 | FastAPI + LangChain |
| 实现路由Agent | 意图识别 + 简单分发 |
| 实现数据分析Agent | 查询销量、库存 |
| 接入LangSmith | 基础监控 |
| 生成模拟数据 | 测试用 |

### Phase 2: 扩展功能（2-4周）

**目标：** 覆盖主要场景

| 任务 | 说明 |
|------|------|
| 客服处理Agent | 订单查询、物流追踪 |
| 选品推荐Agent | 趋势分析 |
| 接入Redis | 短期记忆 |
| 权限系统 | 角色校验 |

### Phase 3: 完善优化（4-8周）

**目标：** 生产可用

| 任务 | 说明 |
|------|------|
| 接入Milvus | 长期记忆 |
| 内容创作Agent | 文案生成 |
| 前端界面 | 卖家后台、客服工作台 |
| 性能优化 | 并发、缓存 |

---

## 十三、数据来源

### 13.1 业务数据

| 阶段 | 数据来源 | 用途 |
|------|----------|------|
| Phase 1 | 模拟数据 | 架构验证 |
| Phase 2 | 公开数据集 | 真实性验证 |
| Phase 3 | 爬虫（小规模） | 竞品分析 |
| Phase 4 | 对接真实平台 | 生产使用 |

### 13.2 知识库数据

| 类型 | 内容 | 来源 | 更新频率 |
|------|------|------|----------|
| **商品知识** | 商品详情、参数、说明书 | 商品上架时提取 | 商品上架时 |
| **FAQ** | 常见问题及答案 | 客服积累 + 人工整理 | 每周更新 |
| **政策规则** | 退换货政策、平台规则 | 平台文档 | 政策变更时 |
| **话术模板** | 客服回复模板 | 优秀客服案例 | 持续积累 |

### 13.3 商品向量化

**流程：**

```
商品数据（PostgreSQL）
    │
    ▼
提取关键信息（名称、描述、特性、场景、价格）
    │
    ▼
构造向量化文本
    │
    ▼
Embedding 模型（BGE-M3）
    │
    ▼
存入 Milvus product_vectors
```

**向量化文本示例：**
```
ProSound降噪耳机 主动降噪40dB 适合通勤地铁 无线蓝牙5.3 30小时续航 价格269元
```

### 13.4 知识库构建

**Phase 1：基础数据**
- 100个模拟商品（含向量化）
- 50条FAQ
- 退换货政策文档

**Phase 2：扩充数据**
- 500个商品
- 200条FAQ
- 客服话术模板

**Phase 3：持续积累**
- 真实商品数据
- 用户反馈的FAQ
- 优秀客服案例

---

*文档基于2026年9月讨论整理，后续根据开发进度持续更新。*
